# estimate-before-build bundle patch: registers the skill with DSH.
# Ships SKILL.md only — no host-process code, no browser client.
- insert:
    - id: estimate-before-build
      name: 'estimate-before-build'
